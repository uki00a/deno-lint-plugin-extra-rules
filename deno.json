{
  "tasks": {
    "check": "deno fmt --check && deno lint",
    "test": "deno test --allow-read=. test.ts"
  },
  "lint": {
    "rules": {
      "include": [
        "no-console"
      ]
    }
  },
  "exclude": ["./tmp"]
}
