{
  "tasks": {
    "check": "deno fmt --check && deno lint",
    "test": "deno test --allow-read=. test.js"
  },
  "lint": {
    "rules": {
      "include": [
        "no-console"
      ]
    }
  },
  "exclude": ["./tmp"]
}
